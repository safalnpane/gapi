{
  "default": "localhost",
  "servers": [
    {
      "name": "localhost",
      "description": "Local API server",
      "base_url": "http://localhost:8080",
      "headers": [
        {
          "Content-Type": "application/json"
        }
      ]
    }
  ]
}
